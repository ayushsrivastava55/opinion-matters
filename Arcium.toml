[toolchain]
anchor_version = "0.32.1"
cluster = "devnet"
package_manager = "yarn"

[network]
cluster = "devnet"
cluster_offset = 768109697
rpc_url = "https://devnet.helius-rpc.com/?api-key=cac5129f-2030-484b-b9c9-6776f80ce9e7"

[localnet]
nodes = 2
localnet_timeout_secs = 300
backends = ["Cerberus"]
# Required when backends includes "Manticore"
# Each path is copied to corresponding node's artifacts/manticore/computation_folder_N
# Example: manticore_data_folders = ["./data/player-0", "./data/player-1"]
# manticore_data_folders = []

[features]
resolution = true
skip-lint = false

[programs.localnet]
private_markets = "FxUZ9r65C8RJDSuHSmiryVWUx9ffeWAX9392iuHCxKr7"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"

[build]
target = "bpfel-unknown-unknown"
